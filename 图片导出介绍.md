# 图片导出技术方案介绍

## 核心技术栈

| 库 | 版本 | 用途 |
|---|---|---|
| `html2canvas` | ^1.4.1 | 将 HTML DOM 元素转换为 Canvas 图像 |
| `jspdf` | ^4.2.1 | 将 Canvas 图像生成 PDF 文件 |

## 实现原理

**整体流程**：
```
HTML 元素 → html2canvas 截图为 Canvas → 导出 PNG / 转 PDF
```

### 1. 导出 PNG

```typescript
import html2canvas from 'html2canvas';

export async function exportToPNG(elementId: string, filename: string) {
  // ① 根据 ID 获取 DOM 元素
  const element = document.getElementById(elementId);

  // ② html2canvas 将 DOM 渲染为 Canvas
  const canvas = await html2canvas(element, {
    scale: 2,                    // 分辨率缩放倍数，2x 即 Retina 高清
    backgroundColor: '#ffffff',  // 背景色，避免透明黑底
    useCORS: true,               // 允许跨域图片资源
    logging: false,              // 关闭控制台日志
  });

  // ③ 通过 dataURL 触发浏览器下载
  const link = document.createElement('a');
  link.download = `${filename}_${dateStr}.png`;
  link.href = canvas.toDataURL('image/png');
  link.click();
}
```

### 2. 导出 PDF

```typescript
import html2canvas from 'html2canvas';
import jsPDF from 'jspdf';

export async function exportToPDF(elementId: string, filename: string) {
  // ① 同样先用 html2canvas 截图
  const canvas = await html2canvas(element, {
    scale: 2,
    backgroundColor: '#ffffff',
    useCORS: true,
    logging: false,
  });

  // ② 计算 A4 纸参数（210mm × 297mm）
  const pageWidth = 210;
  const pageHeight = 297;
  const imgWidth = pageWidth;
  const imgHeight = (canvas.height * imgWidth) / canvas.width;  // 等比缩放

  // ③ 创建 PDF 并分页
  const pdf = new jsPDF('p', 'mm', 'a4');  // 纵向、毫米单位、A4纸张
  let heightLeft = imgHeight;
  let position = 0;

  pdf.addImage(canvas.toDataURL('image/png'), 'PNG', 0, position, imgWidth, imgHeight);
  heightLeft -= pageHeight;

  // ④ 循环添加续页
  while (heightLeft > 0) {
    position = heightLeft - imgHeight;
    pdf.addPage();
    pdf.addImage(canvas.toDataURL('image/png'), 'PNG', 0, position, imgWidth, imgHeight);
    heightLeft -= pageHeight;
  }

  pdf.save(`${filename}_${dateStr}.pdf`);
}
```

## 使用方式

在调用前会先开启「截图模式」隐藏工具栏和提示控件，确保导出干净的内容：

```typescript
const handleExportPNG = async () => {
  // 保存原状态
  const originalScreenshotMode = project.showScreenshotMode;
  // 开启截图模式，隐藏 UI 控件
  setProject(prev => ({ ...prev, showScreenshotMode: true }));
  await new Promise(resolve => setTimeout(resolve, 100)); // 等待 DOM 渲染
  // 执行截图
  await exportToPNG('calendar-content', `${project.title}.png`);
  // 恢复状态
  setProject(prev => ({ ...prev, showScreenshotMode: originalScreenshotMode }));
};
```

## 复用到其他项目的关键步骤

1. **安装依赖**：`npm install html2canvas jspdf`

2. **准备目标元素**：给要截图的 DOM 容器设置唯一 `id`

3. **调用导出**：
   ```typescript
   import { exportToPNG, exportToPDF } from './exportUtils';
   
   exportToPNG('my-container-id', '文件名');
   exportToPDF('my-container-id', '文件名');
   ```

## 注意事项

- **`scale` 参数**：控制截图清晰度，值越大分辨率越高，文件也越大，推荐 `2`
- **`backgroundColor`**：必须设置，否则导出的图片背景会是黑色的
- **跨域图片**：如果 DOM 中有外链图片，需要 `useCORS: true`，且图片服务器支持 CORS
- **字体问题**：自定义字体可能在截图时未加载完成，建议在截图前加 `setTimeout` 等待
- **滚动区域**：`html2canvas` 只能截当前可见区域，如果内容在滚动容器中需要特别处理
